import '../Styles/Modal.scss'

const Modal = () => {
    return (
        <div className="modal">
            <p>Due to bandwidth limits, your service is stopped.</p>
        </div>
    )
}

export default Modal